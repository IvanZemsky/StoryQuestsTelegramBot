export type Story = {
   _id: string
   name: string
   description: string
   img: string
   author: string
   tags: string[]
   sceneCount: number
   passes: number
   likes: number
   date: string
}